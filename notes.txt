CharacterData.js NOTES:
- Planet or Species API data pull:
    if({character.homeworld} === `url`) {
        getHomeworldData();
    } else {
        return {character.homeworld} = "Homeworld unknown"
    }

getHomeworldData():
- Create new file called 'HomeworldData.js'
- Create function:
    const homeworldApiData = 'http https://swapi.dev/api/planets/';

    export default function HomeworldData({ setData }) {
	useEffect(() => {
		axios
			.get(homeworldApiData)
			.then((response) => {
				console.log(response.data);
				return response.data.results;
			})
			.then((planet) => setData(planet))
			.catch((error) => {
				console.log(error);
			});
	}, []);
}
